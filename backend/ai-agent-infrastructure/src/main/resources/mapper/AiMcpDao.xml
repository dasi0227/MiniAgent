<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dasi.infrastructure.persistent.dao.IAiMcpDao">
    <resultMap id="dataMap" type="com.dasi.infrastructure.persistent.po.AiMcp">
        <id column="id" property="id"/>
        <result column="mcp_id" property="mcpId"/>
        <result column="mcp_name" property="mcpName"/>
        <result column="mcp_type" property="mcpType"/>
        <result column="mcp_config" property="mcpConfig"/>
        <result column="mcp_desc" property="mcpDesc"/>
        <result column="mcp_timeout" property="mcpTimeout"/>
        <result column="mcp_chat" property="mcpChat"/>
        <result column="mcp_from" property="mcpFrom"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="queryByMcpId" resultMap="dataMap">
        SELECT id,
               mcp_id,
               mcp_name,
               mcp_type,
               mcp_config,
               mcp_desc,
               mcp_timeout,
               mcp_chat,
               mcp_from,
               create_time,
               update_time
        FROM ai_mcp
        WHERE mcp_id = #{mcpId}
          AND mcp_from = 0
    </select>

    <select id="queryByMcpIdWithFrom" resultMap="dataMap">
        SELECT id,
               mcp_id,
               mcp_name,
               mcp_type,
               mcp_config,
               mcp_desc,
               mcp_timeout,
               mcp_chat,
               mcp_from,
               create_time,
               update_time
        FROM ai_mcp
        WHERE mcp_id = #{mcpId}
        <choose>
            <when test="userId != null">
                AND mcp_from IN (0, #{userId})
            </when>
            <otherwise>
                AND mcp_from = 0
            </otherwise>
        </choose>
        ORDER BY mcp_from DESC, id DESC
        LIMIT 1
    </select>

    <select id="queryByMcpIdByOwner" resultMap="dataMap">
        SELECT id,
               mcp_id,
               mcp_name,
               mcp_type,
               mcp_config,
               mcp_desc,
               mcp_timeout,
               mcp_chat,
               mcp_from,
               create_time,
               update_time
        FROM ai_mcp
        WHERE mcp_id = #{mcpId}
          AND mcp_from = #{userId}
        LIMIT 1
    </select>

    <select id="queryByMcpIdList" parameterType="java.util.List" resultMap="dataMap">
        SELECT id,
               mcp_id,
               mcp_name,
               mcp_type,
               mcp_config,
               mcp_desc,
               mcp_timeout,
               mcp_chat,
               mcp_from,
               create_time,
               update_time
        FROM ai_mcp
        WHERE mcp_chat = 1
          AND mcp_from = 0
          AND mcp_id IN
        <foreach collection="mcpIdList" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <select id="queryByMcpIdListWithFrom" resultMap="dataMap">
        SELECT id,
               mcp_id,
               mcp_name,
               mcp_type,
               mcp_config,
               mcp_desc,
               mcp_timeout,
               mcp_chat,
               mcp_from,
               create_time,
               update_time
        FROM ai_mcp
        WHERE mcp_chat = 1
          AND mcp_id IN
        <foreach collection="mcpIdList" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        <choose>
            <when test="userId != null">
                AND mcp_from IN (0, #{userId})
            </when>
            <otherwise>
                AND mcp_from = 0
            </otherwise>
        </choose>
        ORDER BY mcp_from DESC, id DESC
    </select>

    <select id="queryChatMcpList" resultMap="dataMap">
        SELECT id,
               mcp_id,
               mcp_name,
               mcp_type,
               mcp_config,
               mcp_desc,
               mcp_timeout,
               mcp_chat,
               mcp_from,
               create_time,
               update_time
        FROM ai_mcp
        WHERE mcp_chat = 1
        <choose>
            <when test="userId != null">
                AND mcp_from IN (0, #{userId})
            </when>
            <otherwise>
                AND mcp_from = 0
            </otherwise>
        </choose>
        ORDER BY mcp_from DESC, update_time DESC, id DESC
    </select>

    <select id="queryVisibleList" resultMap="dataMap">
        SELECT id,
               mcp_id,
               mcp_name,
               mcp_type,
               mcp_config,
               mcp_desc,
               mcp_timeout,
               mcp_chat,
               mcp_from,
               create_time,
               update_time
        FROM ai_mcp
        <where>
            <choose>
                <when test="userId != null">
                    AND mcp_from IN (0, #{userId})
                </when>
                <otherwise>
                    AND mcp_from = 0
                </otherwise>
            </choose>
            <if test="idKeyword != null and idKeyword != ''">
                AND mcp_id LIKE CONCAT('%', #{idKeyword}, '%')
            </if>
            <if test="nameKeyword != null and nameKeyword != ''">
                AND mcp_name LIKE CONCAT('%', #{nameKeyword}, '%')
            </if>
        </where>
        ORDER BY mcp_from DESC, update_time DESC, id DESC
    </select>

    <select id="page" resultMap="dataMap">
        SELECT id,
               mcp_id,
               mcp_name,
               mcp_type,
               mcp_config,
               mcp_desc,
               mcp_timeout,
               mcp_chat,
               mcp_from,
               create_time,
               update_time
        FROM ai_mcp
        <where>
            AND mcp_from = 0
            <if test="idKeyword != null and idKeyword != ''">
                AND mcp_id LIKE CONCAT('%', #{idKeyword}, '%')
            </if>
            <if test="nameKeyword != null and nameKeyword != ''">
                AND mcp_name LIKE CONCAT('%', #{nameKeyword}, '%')
            </if>
        </where>
        ORDER BY update_time DESC, id DESC
        <if test="offset != null and size != null">
            LIMIT #{offset}, #{size}
        </if>
    </select>

    <select id="count" resultType="java.lang.Integer">
        SELECT COUNT(1)
        FROM ai_mcp
        <where>
            AND mcp_from = 0
            <if test="idKeyword != null and idKeyword != ''">
                AND mcp_id LIKE CONCAT('%', #{idKeyword}, '%')
            </if>
            <if test="nameKeyword != null and nameKeyword != ''">
                AND mcp_name LIKE CONCAT('%', #{nameKeyword}, '%')
            </if>
        </where>
    </select>

    <select id="countAll" resultType="java.lang.Integer">
        SELECT COUNT(1)
        FROM ai_mcp
        WHERE mcp_from = 0
    </select>

    <select id="queryById" resultMap="dataMap">
        SELECT id,
               mcp_id,
               mcp_name,
               mcp_type,
               mcp_config,
               mcp_desc,
               mcp_timeout,
               mcp_chat,
               mcp_from,
               create_time,
               update_time
        FROM ai_mcp
        WHERE id = #{id}
          AND mcp_from = 0
    </select>

    <select id="queryByIdWithOwner" resultMap="dataMap">
        SELECT id,
               mcp_id,
               mcp_name,
               mcp_type,
               mcp_config,
               mcp_desc,
               mcp_timeout,
               mcp_chat,
               mcp_from,
               create_time,
               update_time
        FROM ai_mcp
        WHERE id = #{id}
          AND mcp_from = #{userId}
    </select>

    <insert id="insert" parameterType="com.dasi.infrastructure.persistent.po.AiMcp">
        INSERT INTO ai_mcp (mcp_id,
                            mcp_name,
                            mcp_type,
                            mcp_config,
                            mcp_desc,
                            mcp_timeout,
                            mcp_chat,
                            mcp_from)
        VALUES (#{mcpId},
                #{mcpName},
                #{mcpType},
                #{mcpConfig},
                #{mcpDesc},
                #{mcpTimeout},
                #{mcpChat},
                <choose>
                    <when test="mcpFrom != null">#{mcpFrom}</when>
                    <otherwise>0</otherwise>
                </choose>)
    </insert>

    <update id="update" parameterType="com.dasi.infrastructure.persistent.po.AiMcp">
        UPDATE ai_mcp
        <set>
            <if test="mcpId != null">mcp_id = #{mcpId},</if>
            <if test="mcpName != null">mcp_name = #{mcpName},</if>
            <if test="mcpType != null">mcp_type = #{mcpType},</if>
            <if test="mcpConfig != null">mcp_config = #{mcpConfig},</if>
            <if test="mcpDesc != null">mcp_desc = #{mcpDesc},</if>
            <if test="mcpTimeout != null">mcp_timeout = #{mcpTimeout},</if>
            <if test="mcpChat != null">mcp_chat = #{mcpChat},</if>
        </set>
        WHERE id = #{id}
          AND mcp_from = 0
    </update>

    <update id="updateByOwner" parameterType="com.dasi.infrastructure.persistent.po.AiMcp">
        UPDATE ai_mcp
        <set>
            <if test="mcpName != null">mcp_name = #{mcpName},</if>
            <if test="mcpType != null">mcp_type = #{mcpType},</if>
            <if test="mcpConfig != null">mcp_config = #{mcpConfig},</if>
            <if test="mcpDesc != null">mcp_desc = #{mcpDesc},</if>
            <if test="mcpTimeout != null">mcp_timeout = #{mcpTimeout},</if>
            <if test="mcpChat != null">mcp_chat = #{mcpChat},</if>
        </set>
        WHERE id = #{id}
          AND mcp_from = #{mcpFrom}
    </update>

    <update id="toggleByOwner" parameterType="com.dasi.infrastructure.persistent.po.AiMcp">
        UPDATE ai_mcp
        SET mcp_chat = #{mcpChat}
        WHERE id = #{id}
          AND mcp_from = #{mcpFrom}
    </update>

    <delete id="delete">
        DELETE
        FROM ai_mcp
        WHERE id = #{id}
          AND mcp_from = 0
    </delete>

    <delete id="deleteByOwner">
        DELETE
        FROM ai_mcp
        WHERE id = #{id}
          AND mcp_from = #{userId}
    </delete>
</mapper>
